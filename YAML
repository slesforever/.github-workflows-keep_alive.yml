name: Angela Awake Protocol

on:
  schedule:
    - cron: '*/5 * * * *' # 每 5 分鐘自動執行一次，幫 Angela 續命

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send Signal to Angela
        # ⚠️ 請把 glitch-hello-node 換成你實際的 Glitch 專案名稱
        run: curl -s https://glitch-hello-node.glitch.me/
